/**
 * UI 模块导出
 * 统一管理用户交互相关组件
 */

// 导出对话框组件（保持原有功能）
export { UIDialogs } from './dialogs';

// 未来可扩展：若有其他UI组件（如面板、菜单等），可在此集中导出
// 例如：
// export { UIPanel } from './panel';
// export { UIMenu } from './menu';